function plus(n1, n2) {
    return n1 + n2;
}

function argCheck(a1, b1) {
    if(a1 === "test" && b1 !== "temp") {
        return b;
    } else {
        return " " + b + "random text";
    }
}

let numberOfCats = 0;

plus(2, numberOfCats);

argCheck("test", "number")